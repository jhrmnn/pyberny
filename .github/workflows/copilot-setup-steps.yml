name: "Copilot Setup Steps"

# Run on changes to this file so it can be validated, and allow manual dispatch.
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/copilot-setup-steps.yml
  pull_request:
    paths:
      - .github/workflows/copilot-setup-steps.yml

jobs:
  # The job MUST be called `copilot-setup-steps` or it will not be picked up by Copilot.
  copilot-setup-steps:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml

      # Node is required by sphinxcontrib-katex's server-side prerender,
      # which scripts/check.sh and .github/workflows/doc.yaml both run.
      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      # The benchmark extra pulls in tblite (the GFN-xTB backend for
      # berny.solvers.XTBSolver) and pyscf, so both are importable in agent
      # sessions.
      - name: Install project with test, doc, and benchmark extras
        run: pip install -e ".[test,doc,benchmark]"

      # Lint tools used by .github/workflows/lint.yaml so `ruff`, `black`,
      # and `mypy` are available without a network round-trip.
      - name: Install lint tools
        run: pip install ruff black mypy
